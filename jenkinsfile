pipeline {
    agent any
    stages {
        stage('Clean') {
            deleteDir()
        }
        stage('Checkout') {
            checkout scm
        }
        stage('Run test') {
            bat returnStdout: true, script: 'virtualenv venv && venv\\Scripts\\activate.bat && pip install .\\ && pytest'
        }
    }
}