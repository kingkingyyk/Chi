pipeline {
    agent any
    stages {
        stage('Clean up working dir') {
            steps {
                deleteDir()
            }
        }
        stage('Example Deploy') {
            steps {
                bat returnStdout: true, script: '''virtualenv venv &&^
                pip install .\\ &&^
                pytest'''
            }
        }
    }
}